name: 'Auto Assign'
on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: https://github.com/Adrian-AAB/auto-pull-request
        with:
          configuration-path: '.github/auto_assign.yml'


